#include <iostream>
#include "microcipher.h"

using namespace std;

/**
 * Encryption and decryption are the same operation, which we call "process"
 */ 
void microcipher_process(const MCKEY& mckey, const istream& input_stream, 
                         const ostream& output_stream) {
    
}
